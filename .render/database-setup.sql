-- Không cần thiết lập gì vì database đã được cấu hình sẵn trên Render
-- File này chỉ để đánh dấu rằng database đã được thiết lập
SELECT 'Database configuration is handled by migrations' AS message; 