@{
    ViewData["Title"] = "Xóa tài khoản";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0"><i class="fas fa-exclamation-triangle me-2"></i> Xác nhận xóa tài khoản</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <h5><i class="fas fa-exclamation-circle me-2"></i> Cảnh báo!</h5>
                        <p>Thao tác này sẽ <strong>xóa vĩnh viễn</strong> tài khoản của bạn và tất cả dữ liệu liên quan,
                            bao gồm:</p>
                        <ul>
                            <li>Thông tin công ty</li>
                            <li>Các công việc đã đăng</li>
                            <li>Ứng viên đã ứng tuyển</li>
                            <li>Thông tin thanh toán và lịch sử</li>
                        </ul>
                        <p class="mb-0">Hành động này <strong>không thể hoàn tác</strong>. Vui lòng chắc chắn rằng bạn
                            đã sao lưu dữ liệu cần thiết.</p>
                    </div>

                    <div class="mt-4">
                        <h5>Bạn có chắc chắn muốn xóa tài khoản không?</h5>
                        <p>Nhập "XÓA TÀI KHOẢN" vào ô dưới đây để xác nhận.</p>

                        <form asp-action="DeleteAccount" method="post" id="deleteAccountForm">
                            <div asp-validation-summary="All" class="text-danger"></div>
                            <div class="form-group mb-4">
                                <input type="text" id="confirmText" class="form-control"
                                    placeholder="Nhập XÓA TÀI KHOẢN để xác nhận" required />
                            </div>
                            <div class="d-flex justify-content-between">
                                <a asp-action="Index" class="btn btn-outline-secondary">
                                    <i class="fas fa-arrow-left me-2"></i> Quay lại
                                </a>
                                <button type="submit" class="btn btn-danger" id="deleteBtn" disabled>
                                    <i class="fas fa-trash me-2"></i> Xóa tài khoản vĩnh viễn
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const confirmText = document.getElementById('confirmText');
            const deleteBtn = document.getElementById('deleteBtn');
            const expectedText = 'XÓA TÀI KHOẢN';

            confirmText.addEventListener('input', function () {
                deleteBtn.disabled = this.value !== expectedText;
            });

            document.getElementById('deleteAccountForm').addEventListener('submit', function (e) {
                if (confirmText.value !== expectedText) {
                    e.preventDefault();
                    alert('Vui lòng nhập chính xác "XÓA TÀI KHOẢN" để xác nhận.');
                }
            });
        });
    </script>
}